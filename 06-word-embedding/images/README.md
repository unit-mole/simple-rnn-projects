# Application Images

The four PNG files are representative portfolio previews generated from the saved model
and application outputs. After deployment, they can be replaced with direct screenshots
from the live Streamlit application while keeping the same filenames.
